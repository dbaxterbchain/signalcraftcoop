export enum DesignStatus {
  Draft = 'draft',
  InReview = 'in-review',
  ChangesRequested = 'changes-requested',
  Approved = 'approved',
}

export enum DesignReviewStatus {
  Approved = 'approved',
  ChangesRequested = 'changes-requested',
}
